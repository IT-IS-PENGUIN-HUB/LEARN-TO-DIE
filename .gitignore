ma token github.txt
.DS_Store
Thumbs.db
node_modules/
dist/
.gemini/
__pycache__/

# Chỗ để tạm file thầy gửi. Nội dung sau khi xử lý đã nằm trong repo:
#   テキスト/  -> public/textbooks/   (PDF giữ nguyên byte, chỉ đổi tên sang ASCII)
#   SCHEDULE/  -> src/assets/schedule/
# Commit cả hai chỗ là nhân đôi 14MB vĩnh viễn trong lịch sử git mà không thêm bản backup nào.
# Phải có dấu / đầu dòng: git trên Windows không phân biệt hoa thường, thiếu nó thì
# "SCHEDULE/" nuốt luôn src/assets/schedule/ và app mất sạch ảnh lịch khi deploy.
/テキスト/
/SCHEDULE/

# Kho đề GỐC kéo từ app của lớp + bộ công cụ Python + mock/plan: để ngoài repo.
# questions.jsonl 14MB và viewer.html 14MB không có lý do gì phải nằm trong lịch sử git —
# bản dùng cho app đã được scripts/build_examdata.py sinh ra ở public/exam-data/ (gọn hơn,
# đã bỏ các trường thừa). Muốn dựng lại public/exam-data/ thì cần thư mục này.
/kho-de-thi/

# Đáp án chính thức tải từ 日本技術士会 (9 PDF, ~2.7MB) — chỉ dùng để đối chiếu bằng
# kho-de-thi/check_official.py, app không cần. Tải lại từ trang ban tổ chức lúc nào cũng được.
/DAP AN（基礎・適性・建設部門）/

# public/exam-data/ CÓ commit và CÓ deploy — quyết định của ロン 13/8/2026:
# repo giữ public để dùng GitHub Pages + sync PC↔iPhone; sau khi thi đỗ sẽ chuyển private.
# Lưu ý: bản dịch tiếng Việt và lời giải là công sức của trung tâm 日越建設交流会.
